/**
 * @astor/design-tokens — fuente de verdad visual (Atlassian-derived).
 *
 * Entradas:
 *   '@astor/design-tokens'          → tokens + tipos (agnóstico de plataforma)
 *   '@astor/design-tokens/css'      → buildThemeCss(), cssVar(), shadowVar() (web)
 *   '@astor/design-tokens/tailwind' → astorPreset (web)
 *   '@astor/design-tokens/mobile'   → theme + ThemeProvider (React Native)
 */
export * from './tokens';
